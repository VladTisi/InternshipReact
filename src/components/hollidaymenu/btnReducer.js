// export default function reducer(state, action) {
//     let newState;
//     switch (action.type) {
//       case 'aprobate':
//         newState = { counter: state.counter + 1 };
//         break;
//       case 'refuzate':
//         newState = { state.filter(state=> state.status =="Aprobat")};
//         break;
//       default:
//         throw new Error();
//     }
//     return newState;
//   }

  